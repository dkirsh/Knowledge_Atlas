# Phase 2: Infinigen Indoors Coverage Survey

| Room Type | Top-Level Entity Hierarchy | Top-3 Parameters (Units, Ranges) | Visual Effect | Observation |
| :--- | :--- | :--- | :--- | :--- |
| **Bedroom** | Bed, Wardrobe, SideTable, Rug | 1. `ceiling_height_m` (m, 2.0-4.0)<br>2. `lighting_warmth` (frac, 0-1)<br>3. `enclosure` (frac, 0-1) | Alters room volume, ambient light color, and perceived tightness of the space. | The bedroom class relies heavily on the bed entity as the focal anchor. Changes to the room's bounding box automatically scale the wardrobe and rug placements to prevent clipping, showing a rigid adherence to functional clearance zones. |
| **Bathroom** | Toilet, SinkVanity, Shower/Tub, Mirror | 1. `glass_area_fraction` (frac, 0-0.6)<br>2. `fixture_count` (int, 1-6)<br>3. `tile_organicism` (frac, 0-1) | Changes reflective surface density, populates/removes fixtures, transitions walls from ceramic to stone. | Bathrooms exhibit strict zoning constraints. The procedural logic prioritizes plumbing adjacency, often clustering the toilet, sink, and shower against a single "wet wall" before populating the rest of the available volume. |
| **Kitchen** | Cabinets, Appliances, Countertop, Island | 1. `counter_height_m` (m, 0.8-1.0)<br>2. `layout_type` (enum, [L, U, Galley])<br>3. `cabinet_wood_ratio` (frac, 0-1) | Shifts vertical work planes, radically alters floorplan shape, swaps synthetic materials for wood grains. | The kitchen is the most computationally dense room due to nested hierarchies (e.g., cabinets contain drawers, which contain handles). The `layout_type` acts as a macro-parameter that overrides minor spatial parameters to enforce structural realism. |
| **Living Room** | Sofa, TV_Stand, CoffeeTable, WindowTreatment | 1. `seating_capacity` (int, 2-8)<br>2. `rug_size_m2` (m², 2-12)<br>3. `tv_screen_in` (in, 32-85) | Expands seating footprint, scales floor covering, alters the primary focal point's mass. | Living rooms use a "conversational radius" logic. Seating entities spawn facing a central locus (usually the TV or coffee table). If `seating_capacity` exceeds the room volume, the script opts to shrink the sofa scale rather than delete seats. |
| **Dining Room** | DiningTable, Chairs, Chandelier, Sideboard | 1. `chair_count` (int, 2-12)<br>2. `table_shape` (enum, [Round, Rect])<br>3. `chandelier_drop_m` (m, 0.5-1.5) | Populates seating array, alters central collision box, changes vertical lighting origin. | This room class is highly symmetrical. The table dictates the exact center of the navigable mesh, and the chandelier dynamically raycasts to ensure it hangs exactly over the table regardless of the ceiling height parameter. |
| **Hallway** | Doors, WallArt, LightFixtures, Trim | 1. `hallway_width_m` (m, 0.9-2.5)<br>2. `length_m` (m, 2.0-15.0)<br>3. `art_density` (frac, 0-1) | Widens the navigable path, extends the corridor, populates frames on blank wall space. | Hallways act as connective tissue rather than destination rooms. The logic explicitly avoids placing any objects with collision meshes in the center of the floor, reserving all decoration for the Z-axis walls to maintain pathfinding. |
| **Office** | Desk, OfficeChair, Bookshelf, Monitor | 1. `desk_width_m` (m, 1.0-2.2)<br>2. `shelf_count` (int, 1-5)<br>3. `monitor_count` (int, 1-3) | Expands the primary work surface, stacks vertical storage, populates the desk surface. | Offices prioritize the desk placement near windows for simulated daylighting if a window entity exists. The monitor count directly influences the clutter generation on the desk surface, simulating functional density. |
